import AppKit

enum ClipboardUIConfig {

    // Window
    static let windowWidth: CGFloat = 180
    static let minWindowHeight: CGFloat = 0
    static let maxWindowHeight: CGFloat = 291

    // Row
    static let rowHeight: CGFloat = 40
    static let rowSpacing: CGFloat = 5

    // Padding
    static let verticalPadding: CGFloat = 10 * 2
}
